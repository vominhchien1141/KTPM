server:
  port: 8082

spring:
  redis:
    host: localhost
    password: 
    port: 6379
    lettuce:
      shutdown-timeout: 30000
      pool:
        max-active: 30
        max-idle: 8
        min-idle: 0